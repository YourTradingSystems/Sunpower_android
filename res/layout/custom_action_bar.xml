<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:paddingTop="5dp"
    android:paddingBottom="5dp"
    android:background="@color/bg_screen_home">

    <Button
        android:id="@+id/btnHome_CAb"
        style="@style/TextViewBlackTitle"
        android:drawableLeft="@drawable/custom_radio_button"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:background="@drawable/background_action_bar_button"
        android:text="@string/action_bar_home_text"
        />

    <Button
        android:id="@+id/btnWork_CAb"
        style="@style/TextViewBlackTitle"
        android:layout_alignParentRight="true"
        android:drawableLeft="@drawable/custom_radio_button"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:background="@drawable/background_action_bar_button"
        android:text="@string/action_bar_faire_text"
        />

</RelativeLayout>